name: flutter_cert
description: A simple command-line to generate TrustedCertificatesBytes for flutter http.
# version: 1.0.0

environment:
  sdk: '>=2.10.0 <3.0.0'

dependencies:
  path: ^1.7.0
  args: ^2.0.0
    
dev_dependencies:
  pedantic: ^1.9.0
